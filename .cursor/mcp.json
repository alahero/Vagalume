{
  "mcpServers": {
    "stitch": {
      "command": "npx",
      "args": ["-y", "@_davideast/stitch-mcp", "proxy", "--transport", "stdio"],
      "envFile": "${workspaceFolder}/.env.stitch"
    }
  }
}
