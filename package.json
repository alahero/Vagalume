{
  "name": "vagalume",
  "private": true,
  "description": "Raíz del monorepo; la app vive en web/. En Vercel: Settings → General → Root Directory = web (obligatorio; el vercel.json activo es web/vercel.json)."
}
