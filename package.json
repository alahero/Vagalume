{
  "name": "vagalume",
  "private": true,
  "description": "Raíz del monorepo; la app vive en web/. Vercel: Root Directory vacío (raíz del repo); vercel.json en esta raíz usa npm --prefix web.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web"
  }
}
