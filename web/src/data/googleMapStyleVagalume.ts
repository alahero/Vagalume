/**
 * Misma data que `googleMapStyleVagalume.json` (JSON puro para pegar en editores de estilo).
 * En la app: `import { VAGALUME_MAP_STYLES } from '...'` o pega el contenido del `.json` en la herramienta.
 */
import vagalumeMapStyles from "./googleMapStyleVagalume.json";

export const VAGALUME_MAP_STYLES = vagalumeMapStyles;
