/**
 * Eventos MandalaTickets (Vagalume) vía /api/mt-venues.
 * Reexporta el contexto cargado en SiteLayout.
 */
export {
  useVagalumeMtEvents,
  useNearestMtEvents,
  VagalumeMtEventsProvider,
} from "../context/VagalumeMtEventsContext";
